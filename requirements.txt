# LangChain Internship Assignment – Innomatics Research Labs, Feb 2026
# Author: Harsha Devupalli
#
# Install all at once:
#   pip install -r requirements.txt
#
# Or on Google Colab:
#   !pip install -r requirements.txt

# ── LangChain Core ──────────────────────────────────────────────────
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0
langchainhub>=0.1.21

# ── Vector Store ────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Embeddings (free local model) ───────────────────────────────────
sentence-transformers>=3.0.0

# ── Text splitting (included in langchain, but pinning explicitly) ──
langchain-text-splitters>=0.3.0

# ── Standard library (no install needed, but listed for clarity) ───
# getpass  ← built-in
# os       ← built-in
# datetime ← built-in
# math     ← built-in
