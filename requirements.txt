
# Core LangChain packages
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0

# Embeddings (free, runs locally)
sentence-transformers>=3.0.0

# Vector store (local, no server needed)
chromadb>=0.5.0

# LangChain Hub (for pre-built agent prompts)
langchainhub>=0.1.21

# Jupyter support (for running locally)
jupyter>=1.0.0
ipykernel>=6.0.0

# Utilities
python-dotenv>=1.0.0
